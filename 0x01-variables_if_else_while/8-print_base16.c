#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - entry point
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	char a = (48);
	char b = (97);

	for (a = (48); a <= (57); a++)
	{
		putchar (a);
	}
	for (b = (97); b <= (102); b++)
	{
		putchar (b);
	}
	return (0);	
}
